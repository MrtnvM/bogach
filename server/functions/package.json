{
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "yarn build && firebase serve --only functions",
    "shell": "yarn build && firebase functions:shell",
    "start": "yarn shell",
    "deploy-staging": "firebase deploy -P staging --only functions",
    "deploy-uat": "firebase deploy -P uat --only functions",
    "deploy-production": "firebase deploy -P production --only functions",
    "start-emulator": "yarn build && firebase emulators:start",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@firebase/app-types": "0.x",
    "@types/node-fetch": "^2.5.7",
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0",
    "immer": "^6.0.3",
    "node-fetch": "^2.6.1",
    "random": "^2.2.0",
    "sha256": "^0.2.0",
    "ts-mockito": "^2.5.0",
    "uuid": "^3.4.0"
  },
  "devDependencies": {
    "@types/jest": "^25.1.4",
    "@types/node": "^14.0.13",
    "@types/random": "^2.1.1",
    "@types/sha256": "^0.2.0",
    "@types/uuid": "^3.4.7",
    "firebase-functions-test": "^0.1.6",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "tslint": "^5.12.0",
    "typescript": "^3.8.3"
  },
  "private": true
}

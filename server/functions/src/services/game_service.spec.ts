/// <reference types="@types/jest"/>

// import { mock, instance } from 'ts-mockito';
// import { GameProvider } from '../providers/game_provider';
// import { GameService } from './game_service';

describe('Test Game Service', () => {
  test('Can not generate game events with missing game ID', () => {
    // const mockGameProvider: GameProvider = mock(GameProvider);
    // const gameProvider: GameProvider = instance(mockGameProvider);
    // const gameService = new GameService(gameProvider);
    // const generate = gameService.generateGameEvents;
    // expect(generate('')).toThrow('Missing Game ID');

    expect(true).toBeTruthy();
  });
});
